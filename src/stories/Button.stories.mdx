import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs/blocks";
import Button from "../components/Button";

<Meta
	title="Button/MDX"
	component={Button}
	argTypes={{
		text: {
			name: "Button Text",
			type: "string",
			control: "text",
			description: "Button Text",
			table: {
				type: {
					summary: "String Text",
					detail: "Inside button text"
				},
				defaultValue: {
					summary: "Default Text"
				}
			}
		},
		type: {
			name: "Button Type",
			control: {
				type: 'inline-radio',
				options: ['default', 'primary', 'secondary']
			},
			description: "Button Type",
			table: {
				type: {
					summary: "Render Button Type",
					detail: "Render Primary, Secondary or Default Button"
				},
				defaultValue: {
					summary: "default"
				}
			}
		},
	}}
/>

# This is a documentation page for the Button component

export const Template = ({text, type}) => <Button text={text} type={type} />

<Story
	name="Default"
	args={{
		text: "Default Text",
		type: 'default'
	}}
>
	{Template.bind({})}
</Story>

<ArgsTable story='Default' />

# Here are multiple options

<Canvas>
	<Story name="Default 2" args={{
		text: "Default Text 2",
		type: 'default'
	}}>
		{Template.bind({})}
	</Story>
	<Story name="Default 3" args={{
		text: "Default Text 3",
		type: 'primary'
	}}>
		{Template.bind({})}
	</Story>
	<Story name="Default 4" args={{
		text: "Default Text 4",
		type: 'secondary'
	}}>
		{Template.bind({})}
	</Story>
</Canvas>