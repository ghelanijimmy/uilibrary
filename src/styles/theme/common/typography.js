import { base } from "./base";

/**
 * Font styles for theme provider
 * @type {{small: string, default: string, size: string, large: string, extraLarge: string, medium: string, family: string}}
 * @category Styles
 */
export const typography = {
  family: "sans-serif",
  size: `${base.fontSize}px`,
  default: "1em",
  small: ".875em",
  medium: "1.25em",
  large: "1.5em",
  extraLarge: "2em",
};
