/**
 * Base styles
 * @type {{defaultBorderRadius: number, defaultPadding: number, fontSize: number}}
 * @category Styles
 */
export const base = {
  defaultPadding: 20,
  fontSize: 16,
  defaultBorderRadius: 4,
};
