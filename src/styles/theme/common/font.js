import { base } from "./base";

/**
 * Font styles for theme provider
 * @type {{size: string, family: string}}
 * @category Styles
 */
export const font = {
  family: "sans-serif",
  size: `${base.fontSize}px`,
};
