/**
 * Button Type Constants
 * @type {{SECONDARY: string, PRIMARY: string, DEFAULT: string}}
 * @param DEFAULT {'default'}
 * @param PRIMARY {'primary'}
 * @param SECONDARY {'secondary'}
 * @category Constants
 */
export const buttonTypes = {
  PRIMARY: "primary",
  SECONDARY: "secondary",
  DEFAULT: "default",
};

/**
 * Size Constants
 * @type {{XL: string, MD: string, SM: string, LG: string}}
 * @category Constants
 */
export const sizes = {
  SM: "sm",
  MD: "md",
  LG: "lg",
  XL: "xl",
};
